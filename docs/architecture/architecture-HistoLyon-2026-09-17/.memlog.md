---
scope: Socle de développement HistoLyon : architecture applicative (niveau frontières/ownership), structure du repo, architecture documentaire, process d'équipe parallèle, tests/CI, adaptation BMAD
purpose: build-substrate + livrables humains (organisation, bootstrap)
altitude: initiative (keeps features)
mode: coaching
updated: 2026-09-17T15:37
---

- (constraint) Cadre : projet fin d'études Epitech T-ESP-800, année 2 = développement ; jury évalue la GESTION DE PROJET (organisation, avancement quantifié, KPIs, capacité d'ajustement), pas un produit marché
- (constraint) Équipe ~7 personnes temps partiel (~½ journée / 2 semaines / membre), composition instable, rôles flous ; 1 cheffe de projet (pilotage, 1er parcours, admin/légal), 2 spécialistes 3D, le reste dev app ; IDE/IA hétérogènes, pas d'abonnement/BMAD garanti pour tous
- (constraint) Outil de suivi imposé (cadrage an 1) : SharePoint avec page liste de suivi de progression comme ticketing
- (constraint) Input = conception/ (15 paliers YAML + maquette Figma, 41 écrans mobile + 8 admin desktop, ~24 entités, invariants I1–I10) — source de vérité du QUOI ; l'architecture ne la re-documente pas, elle la cite par ID (Dx.y, Ix, écrans)
- (constraint) [ADOPTED] Flutter pour l'app mobile (acté par la conception : 'devs Flutter' partout)
- (constraint) [ADOPTED] App sans compte : Profil local toujours présent, Compte 0..1 opt-in pour publication/sync (I2, I6) ; identité civile jamais collectée (I1) ; modération/validation scientifique (I3, I4) ; pas de dark mode ; pas de turn-by-turn
- (direction) Orientations du porteur : BMAD comme colonne vertébrale mais consommable sans l'installer (principes + prompts + stories dans le repo) ; architecture documentaire 'juste ce qu'il faut' par tâche ; CI/CD avec hooks pre-commit/pre-push ; tests d'abord, vérifiés à chaque commit ; découpe en unités élémentaires (reprise en une session courte)
- (direction) Attentes école : état des lieux quasi automatique depuis le travail réel ; KPIs d'avancement mesurables par construction ; rétrospective/ajustement périodique de l'organisation documenté ; usage IA autorisé si maîtrisé et explicable
- (direction) Le porteur est aujourd'hui goulet (découpe, Figma, arbitrage produit) → répartir les rôles (Pilote, Découpeur, Intégrateur, Gardien tests, Gardien design, Dev, 3D), découpe autonome
- (question) Laissés ouverts par la conception, à trancher ici : moteur carto, étendue hors-ligne, méthode d'auth, format templates contenu + CMS, stockage médias lourds, stack back-office web, moteur 3D/AR, schéma lien universel, state management Flutter
- (event) Activation : intent=create, mode=coaching ; livrables = spine + doc organisation + plan de bootstrap (public : équipe + jury)
- (decision) GitHub = source de vérité du travail (issue = story = branche = PR) ; SharePoint devient une VUE alimentée (export script / jalons), plus la vérité. Binds: suivi, KPIs, CI. Prevents: reporting manuel reconstruit à chaque point. Alternatives écartées: SharePoint seul (pas d'automatisation depuis le travail réel)
- (decision) Backend = Supabase (Postgres+PostGIS, Auth, Storage, RLS, API auto). Binds: toutes entités palier 4, auth, médias. Prevents: une équipe backend dédiée qu'on n'a pas ; logique métier dupliquée mobile/admin. Alternatives écartées: API custom NestJS/FastAPI (coût humain), Firebase (NoSQL mauvais fit géo/relationnel)
- (decision) Pipeline contenu = contenu-en-fichiers (YAML conformes aux templates palier 8, validés en CI, seedés en base) DÈS le bootstrap ; back-office web 'le plus tôt possible' sur le même schéma. Binds: D10, C5, admin. Prevents: 8 écrans admin sur le chemin critique du 1er pin
- (decision) Monorepo unique (apps mobile+admin, supabase, content, docs, conception, prompts). Binds: all. Prevents: contexte agent éclaté, CI multiples, chemins instables dans les stories
- (constraint) Spécialistes 3D : pas graphistes, ne créent pas d'assets (récupération sur internet), s'adaptent à l'archi ; objectif = 1 bâtiment reconstitué dans le 1er parcours → fixer un CONTRAT d'intégration (format, stockage, déclenchement), pas un moteur
- (constraint) Personne dans l'équipe n'a fait de Flutter ni de carto → privilégier le courant dominant et les chemins pavés ; onboarding par la doc du repo
- (decision) Paradigme app = feature-first (features/<domaine Dx>) × couches strictes data/domain/presentation, dépendances vers le bas uniquement, jamais feature→feature sauf via core/. État Riverpod (codegen), navigation go_router, tokens palier 12 repris 1:1. Prevents: deux features avec state/nav incompatibles. Alternative écartée: Bloc (plus cérémonial, personne ne le connaît)
- (decision) Carto = MapLibre (maplibre_gl), un style JSON par époque, tuiles vectorielles OSM (OpenFreeMap/Protomaps), hors-ligne PMTiles. Prevents: styles d'époque impossibles en tuiles image ; dépendance clé API/quota. Alternative écartée: Mapbox (coût, quota, Studio). Versions à vérifier web avant figement
- (decision) Ownership données : (1) schéma = supabase/migrations SQL, seule vérité, invariants I1–I10 en contraintes/RLS testées ; (2) types Dart/TS GÉNÉRÉS, jamais écrits à la main ; (3) serveur possède contenu éditorial+communautaire, terminal possède Profil local (Drift), sync = flux explicite opt-in ; (4) contenu-fichiers validés par schéma dérivé, seed idempotent par id stable
- (decision) Hors-ligne = parcours préchargé seulement (pas tout Lyon) → cache contenu = paquet par parcours (pins, médias, tuiles de l'emprise) dans Drift + fichiers ; la carte hors emprise n'est pas garantie
- (decision) Contrat 3D/AR : assets glTF/GLB dans Supabase Storage, référencés par l'entité Modèle 3D (C1), ouverts via features/immersion/ qui isole le viewer ; moteur (viewer Flutter / WebView / Unity bridge) DIFFÉRÉ, revisite après le 1er spike des spécialistes 3D
- (version) Base locale = Drift (SQLite) ; Hive/Isar écartés (Isar à l'arrêt) — à vérifier web
- (constraint) Capacité réelle : 1 journée école/semaine partagée entre 3 projets → par membre et par 2 semaines : ~3–4 h de dev + ~1 h de réunion d'équipe. Comptes rendus de réunion restent sur SharePoint
- (decision) Unité de travail = story ≤ 3 h (une session), auto-suffisante ; 1 story = 1 issue GitHub = 1 branche = 1 PR ; arbre Domaine Dx → Épic → Story, IDs conception = labels GitHub. Prevents: tâches trop larges pour une session ou un agent ; avancement non mesurable
- (decision) Flux git : trunk-based, main protégé, PR obligatoire, CI verte + 1 relecture par un autre membre, squash merge, branches story/<ID>-<slug>
- (decision) Cadence : itération 2 semaines ; synchro asynchrone = état des lieux généré depuis GitHub ; réunion 1 h/2 sem (CR sur SharePoint) ; rétro d'organisation à chaque rendez-vous école consignée dans docs/team/retros/
- (decision) Architecture documentaire en 4 niveaux : L0 AGENTS.md (racine + par app, <150 lignes, UNIQUE fichier d'instructions, CLAUDE.md/.cursor/copilot = copies générées) ; L1 docs/ (spine, conventions, guides-recettes) ; L2 conception/ jamais chargée entière, adressée par ID stable via index généré + tools/ctx ; L3 story = brief qui CITE les extraits à lire (inline à la demande par le script). Prevents: saturation de contexte, instructions divergentes par IDE
- (decision) BMAD produit, le repo distribue : Découpeur (porteur + binôme) utilise bmad-spec / bmad-create-epics-and-stories ; sorties committées (docs/spec, docs/stories, docs/sprint/sprint-status.yaml) ; format story BMAD = template d'issue GitHub ; prompts/ portables (dev-story, review, découpe) + checklist sans IA ; rôles = casquettes tournantes avec binôme, docs/team/ROLES.md vivant
- (constraint) Correction porteur : NE PAS compter les stories ni calibrer leur taille sur la capacité (disponibilité très variable : semaines pleines à l'école, week-ends de rattrapage) ; ce n'est pas grave si tout n'est pas développé en fin d'année. Story = LA PLUS PETITE unité de travail possible, point
- (decision) Tests d'abord = ORDRE strict : tests écrits (rouges) PUIS implémentation, jamais l'inverse (sinon on adapte les tests au code). Mécanismes : story = section Tests avant Implémentation ; convention 1er commit de la branche = tests ; checklist PR ; Gardien des tests relit l'ordre. Couverture publiée avec cliquet (jamais en baisse)
- (decision) Politique de tests par couche : domain unitaires obligatoires ; data contre Supabase local ; presentation widget tests (pas de golden au début) ; SQL pgTAP par invariant I1–I10 et par policy RLS ; content validation de schéma ; 1 seul integration_test (parcours démo) en nightly
- (decision) CI/CD : lefthook (pre-commit format+lint+schéma contenu ; pre-push tests unitaires app touchée) ; GitHub Actions filtrées par chemin ; environnements local (Supabase CLI) → dev (projet gratuit, migré par CI sur main) → demo (projet gratuit, migré à la main sur tag, vu par le jury) ; secrets en GitHub Environments
- (decision) Distribution : Android uniquement (APK artefact CI) ; iOS DIFFÉRÉ (pas de Mac, pas de licence Apple Developer) — revisite si un Mac + licence apparaissent
- (decision) Back-office = Flutter Web (une seule stack, packages Dart partagés design_tokens / api_types / ui_kit), hébergé statique. Alternative écartée: SPA React (2e stack, 2e génération de types, équipe non identifiée React)
- (decision) Page web publique lecture seule (D11, fallback lien universel, OG payload) DIFFÉRÉE — piste : Edge Function Supabase rendant un HTML minimal ; revisite quand le lien universel est mis en chantier
- (decision) KPIs par construction : tools/report interroge GitHub (issues/PR/labels domaine/dates) + artefacts CI (couverture) → docs/sprint/reports/<date>.md = état des lieux collé dans le CR SharePoint. Prérequis : label domaine obligatoire, issue liée à la PR
- (event) Coaching terminé ; passage à Finalize (vérification web des versions, distillation)
- (version) Vérifié web 2026-09-17 : Flutter stable 3.47.4 (Dart 3.13.3) ; flutter_riverpod 3.4.3 ; go_router 18.0.1 ; drift 2.35.0 ; supabase_flutter 2.17.2 ; maplibre_gl 0.27.1 (publisher maplibre.org, Android/iOS/Web, PMTiles, styles JSON, offline regions) ; melos 8.7.0 ; flutter_lints 6.0.0 ; Supabase CLI v2.117.0 (supabase test db = pgTAP) ; lefthook v2.1.14 ; Isar 3.1.0 figé depuis 2023 (écarté confirmé)
- (constraint) Supabase CLI gen types ne produit PAS de Dart (typescript/go/swift/python seulement) → génération Dart via supadart 1.9.3 (maintenu, petit projet, 107★) ; repli : OpenAPI PostgREST → openapi_generator. La RÈGLE (types jamais écrits à la main) tient, l'outil est seed
- (version) Tuiles : OpenFreeMap et Protomaps (PMTiles) en ligne et actifs — sources de tuiles vectorielles OSM sans clé API
- (event) Spine distillée (18 AD, 12 conventions, stack vérifiée, 3 diagrammes, 10 différés) ; lint_spine 0 finding ; Reviewer Gate lancé : 2 réconciliations + rubrique + fraîcheur + adversaire
- (constraint) Vérifié web 2026-09-17 — Supabase Free : 2 projets actifs max, 500 Mo base, 1 Go Storage, 5 Go egress, pause après 1 semaine d'inactivité, pas de sauvegarde automatique → dev+demo consomment tout le quota ; keep-alive et export hebdo obligatoires ; budget médias par bucket
- (event) Reviewer Gate terminé : 5 rapports dans reviews/ (reconcile-contexte, reconcile-conception, rubric, freshness, adversarial). Verdict global : structure juste, mais ownership multi-écrivains, état de session, enveloppe Supabase, médias, transverses et 3 hypothèses techno à corriger
- (decision) AD-4 amendée (I6, capability link 2026-05-05) : ownership PAR PROVENANCE — serveur = éditorial/communautaire (C1, C2 hors personnel, C4, C5, Lien universel) ; terminal = Profil sans Compte + Parcours personnel + Image/Code visuel générés localement. Partager un Parcours personnel par Lien universel EXIGE un Compte (I6 = acte qui ouvre la sync) → RPC parcours_partager, visibilité non_liste. Fusion sync : union pour Favoris/Historique, last-write-wins pour préférences ; uuid générés côté client partout
- (decision) AD-5 corrigée : composition root apps/*/lib/app/ (router assemblé, bootstrap, ProviderScope) = seul endroit qui importe les features ; core/router = noms/chemins seulement ; chaque feature exporte ses GoRoute ; pas d'inversion domain/data (domain importe data) ; api_types = entités serveur utilisées telles quelles ; packages importables par toutes les couches ; interaction inter-features = navigation par nom + core/session uniquement
- (decision) Nouvelle AD ownership & transitions : table entité → écrivain autorisé par app/feature ; toute transition de statut/provenance = fonction SQL exposée en RPC, avec Trace, testée pgTAP ; colonnes de cycle de vie non modifiables directement (RLS/trigger) ; repository = appelant de la RPC. Logique serveur = SQL/RPC ; Edge Functions uniquement par amendement. Rôles équipe : table membre_equipe(compte_id, role enum editeur|validateur|moderateur) en migration socle ; apps = clé publishable seulement ; clé secrète = CI/tools
- (decision) Nouvelle AD état de session : core/session = liste FERMÉE de providers (epoqueSelectionnee, parcoursActif, etapeCourante, pinCourant, modePresentation, lectureAudio) ; comportements transverses = services core/ (permissions, location, presentation_mode, audio_player, share, offline, db, profile) consommés par les features ; priorité utilisateur vs parcours sur le slider = question ouverte dont core/session est propriétaire
- (decision) Nouvelle AD enveloppe Supabase gratuite : dev + demo = les 2 projets ; Action keep-alive hebdo ; export hebdo db dump + Storage en artefact/release ; budget Storage par bucket ; AD-7 phase 2 conditionnée à l'export automatique en place
- (decision) AD-7 resserrée : seed = upsert-only, provenance=editorial seulement, ne touche jamais statut/traces/lignes communautaires ; content/ exhaustif (pins, parcours, epoques, categories, sources dans le pin, medias = manifeste) ; binaires HORS git : téléversés dans Storage par tools/media depuis un dossier partagé (OneDrive/SharePoint), YAML référence bucket/slug/fichier, CI vérifie l'existence ; formats webp / m4a-AAC / glb, tailles max en CI ; JSON Schema généré par tools/gen-types
- (decision) AD-8/AD-9 corrigées (schémas de tuiles incompatibles) : UNE chaîne de tuiles = lyon.pmtiles (schéma Protomaps basemap, extrait en CI par tools/tiles depuis le build Protomaps, hébergé bucket tiles, servi en ligne par range requests via pmtiles://, téléchargé UNE fois pour le hors-ligne et partagé par tous les paquets de parcours) ; styles d'Époque = fonds Protomaps ; couches de données (pins, tracés) ajoutées par code avec ids de couches identiques dans tous les styles et ré-ajoutées après setStyle ; ajouter une Époque = migration + style dans la même PR ; maplibre_gl ≥ 0.27.1 (0.27.0 cassé sur Flutter ≥ 3.44) ; PMTiles copié sur disque (file://), pas asset://
- (decision) AD-3 amendée : supadart 1.9.3 maintenant (enums à déclarer dans supadart.yaml, PostGIS via geobase — spike geography/enum nullable AVANT la 1re migration) ; bascule vers supabase_typegen officiel (0.1.4) quand supabase_flutter 3 est stable ; conflit git sur fichier généré = régénérer, jamais résoudre à la main
- (decision) AD-16 resserrée : une migration par PR, label migration sérialisé par l'Intégrateur, branche à jour avec main obligatoire, CI fait db reset from scratch sur le commit de fusion, check refusant un timestamp antérieur au dernier de main ; seed dev/demo depuis content/ ; clés nommées sb_publishable_ / sb_secret_
- (decision) AD-10 corrigée : constante = identifiant Figma verbatim avec - → _ (TOK_COLOR_PRIMARY), lint désactivé dans le package, jamais d'alias (un id = une constante), tools/check-tokens compare l'export Figma au package ; Text/Effect styles pareil ; mapping comp-* manuel dans packages/ui_kit/FIGMA-MAP.md (Code Connect indisponible)
- (decision) AD-12/13/14 resserrées : niveau Épic explicite (Domaine → Épic → Story, docs/spec + docs/stories produits avec BMAD par le Découpeur) ; une story = une seule feature OU un seul de supabase/ content/ packages/x ; migration + UI = deux stories, migration d'abord ; itération = 2 semaines ; preuve tests-d'abord = ordre des commits vérifié par le relecteur avant squash + champ PR ; pre-push = analyze+format+tests du package touché
- (decision) AD-17 réécrite (correction porteur) : KPIs indépendants de la taille des stories — sous-domaines Dx.y avec DoD atteint / total par domaine, délai story→fusion, taux de CI verte, couverture, actions de rétro closes ; l'état des lieux LISTE fait/en cours/reste, sans faire du nombre de stories une cible. SharePoint = CR de réunion + rapport collé ; la liste de suivi imposée est remplacée par GitHub Projects (décision porteur)
- (decision) Conventions ajoutées : IA déclarée dans la PR (outil, ce qui a été généré, relecture humaine obligatoire, prompts versionnés dans prompts/) ; i18n = fr seulement (06-i18n), supportedLocales=[fr], pas de contenu multilingue ; pas de dark mode ni turn-by-turn [ADOPTED] ; polymorphisme = (type_de_cible enum, cible_id uuid) + CHECK ; Failure dans core/models ; APK versionné semver + build number CI, clé de signature en secret ; pubspec.lock committé, montées de version = PR label deps ; attribution ODbL OpenStreetMap + Protomaps obligatoire ; licence obligatoire sur Média et Modèle 3D récupérés ; observabilité = logs Supabase + core/log, rien de plus
- (version) Vérifié web 2026-09-17 : riverpod_generator 4.0.9 / riverpod_annotation 4.0.7 / riverpod_lint 3.1.9 (compatibles riverpod 3.4.3) ; build_runner 2.16.1 ; drift_dev 2.35.0 ; drift_flutter 0.3.1 ; supabase_typegen 0.1.4 (officiel, exige supabase_flutter 3.0.0-dev) ; melos 7+ = plus de melos.yaml, workspace Pub dans pubspec.yaml racine
- (event) Triage validé par le porteur : (1) partage d'un Parcours personnel par Lien universel exige un Compte — OK ; (2) rôles serveur editeur|validateur|moderateur OK, jamais assignés à une personne nommée dans la doc ; (3) budgets Storage acceptés
- (direction) Livrables humains à produire : ORGANISATION.md (contraintes → mécanismes → organisation, public équipe + jury) et BOOTSTRAP.md (premières actions sur dépôt vierge jusqu'à 'l'équipe peut prendre une story')
- (event) ORGANISATION.md et BOOTSTRAP.md rédigés ; passage aux doc_standards (bmad-review lenses=structure,prose) sur ces deux documents uniquement
- (event) Revue éditoriale appliquée sur ORGANISATION.md et BOOTSTRAP.md (reviews/editorial-report.md) ; spine status=final, companions=[ORGANISATION.md, BOOTSTRAP.md]
- (event) spine finalized
- (event) Reprise 2026-09-17 (update des companions) : retour porteur — ORGANISATION.md (contraintes → mécanismes, cible jury) illisible pour l'équipe : ne répond pas à 'comment est organisé le repo / qu'est-ce que je fais / comment on se coordonne' ; BOOTSTRAP.md = plan clair mais la suite n'était pas dite
- (decision) ORGANISATION.md REMPLACÉ par un guide d'équipe (même nom de fichier, renvois conservés) : langage courant, zéro AD-n dans le corps, sections = dépôt pièce par pièce / installer son poste / ta première story pas à pas / comment on se coordonne / règles non négociables / pourquoi (court) / glossaire. Le 'pourquoi' pour le jury est réduit à une section ; la spine reste la référence des règles. Plus une page HTML de présentation pour la réunion d'1 h
- (direction) Bootstrap : le porteur suit la méthode BMAD — BOOTSTRAP.md est l'input de bmad-spec (spec du socle, spine en companion) → bmad-create-epics-and-stories (épic socle, stories socle-01…11) → bmad-sprint-planning → bmad-build story par story, dans ce dépôt (remote Projet-decole/Histolyon, 0 commit). Prérequis hors BMAD : § 0 de BOOTSTRAP (Flutter, Supabase CLI, lefthook, melos, gh absents du poste le 2026-09-17)
- (event) ORGANISATION.md réécrit en guide d'équipe (7 sections + glossaire) ; ORGANISATION.html (page de présentation navigable, même contenu) publié : https://claude.ai/artifact/74UgnCwNa5QCS65kdGgNvZ ; renvois de BOOTSTRAP.md mis à jour
- (decision) Correction porteur : le guide d'équipe doit être NARRATIF — une contrainte mène à un principe qui mène à un mécanisme ; le dépôt, la story et les casquettes arrivent en fin comme résultat, chaque élément renvoyant au § dont il découle. Structure retenue : § 0 d'où on part (faits + problème) → § 1–6 'Puisque … : donc …' (technos, petits morceaux, trace, parallèle, qualité, règles trouvables) → § 7–9 résultat (dépôt, première story, équipe) → annexes (poste, règles fixes, glossaire). ORGANISATION.md et ORGANISATION.html réécrits ainsi (artifact v2)
- (decision) Amendements 2026-09-17 depuis bmad-spec (spec-histolyon-socle, Q1–Q3) : socle-01…11 = épics (BOOTSTRAP l.3) ; approbation obligatoire sur main activée après relecture groupée de fin de socle (BOOTSTRAP § 1.1) ; sorties BMAD sous docs/ : specs/, épics dans docs/, stories/ + sprint-status.yaml, architecture/ = ce run (AD-12, arbre) ; _bmad/config.toml repointé
- (event) Spike Story 4.1 (2026-09-21, table jetable supprimée après coup) : supadart 1.9.3 exécuté contre le socle local (`0001_socle.sql`) avec une table jetable `geography(point,4326)` nullable + enum `statut_pin` nullable. **Geography nullable : correct** — `Geometry?` (package `geobase`), `null` bien préservé en lecture/écriture via WKB hex, sans même activer `postGIS: true` dans `supadart.yaml`. **Enum nullable : bug confirmé** — `lib/generators/class/from_json.dart:264` a un retour dédié aux enums qui ignore `columnDetails.isNullable` et retombe toujours sur `$enumName.values.first` quand la valeur JSON est `null`, au lieu de `null` (repro : `STATUT_PIN.values.first` = `brouillon`, silencieux). **Conclusion : supadart retenu** — aucune colonne enum du schéma réel n'est nullable aujourd'hui (`statut`, `provenance`, `role` sont toutes `not null`), donc le bug a un impact nul en l'état ; `tools/gen-types` (Story 4.2) doit néanmoins vérifier par introspection qu'aucune colonne enum n'est nullable et échouer bruyamment sinon, pour empêcher qu'une future migration ne réintroduise ce piège sans qu'on s'en aperçoive.
