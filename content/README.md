Contenu éditorial en YAML (époques, catégories, sources, pins), chargé en base par `dart run tools/seed.dart` (upsert par `slug`) et validé par `dart run tools/validate_content.dart` contre `content/schema/` (généré). Les médias ne sont jamais versionnés ici : seulement leurs chemins dans Supabase Storage.
